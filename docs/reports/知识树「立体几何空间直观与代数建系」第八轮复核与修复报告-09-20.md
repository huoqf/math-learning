# 知识树「立体几何空间直观与代数建系」第八轮复核与修复报告

> 前序：[`知识树「立体几何空间直观与代数建系」专项审查报告-09-20.md`](./知识树「立体几何空间直观与代数建系」专项审查报告-09-20.md)（4 P0 + 14 P1 + 10 P2）
> 、[`知识树「立体几何空间直观与代数建系」专项修复报告-09-20.md`](./知识树「立体几何空间直观与代数建系」专项修复报告-09-20.md)（七轮治理）
> 本轮性质：**独立复核（第八轮）＋ 全量修复闭环**。复核结论见 §一，修复清单见 §二，门禁实测见 §三，旧报告过期陈述回改见 §四，遗留见 §五。
> 修订日期：2026-09-20 12:55 ｜ 代码基线：`HEAD = acc4af6` + 未提交工作树

---

## 一、复核结论（第八轮，逐条对当前文件重新取值）

### 1.1 门禁实测（修复前 / 修复后各跑一次）

| 门禁 | 修复前 | 修复后 | 判定 |
|:---|:---|:---|:---|
| `node node_modules/typescript/bin/tsc -b` | exit 0 | **exit 0** | ✅ |
| `node node_modules/vitest/vitest.mjs run` | 140 文件 / 1326 用例 | **141 文件 / 1328 用例**（+1 文件为新增守护测试） | ✅ |
| `npm run audit:strict` | 714 文件 / 0 错误 / 51 建议 / 增量 0 | **714 文件 / 0 错误 / 51 建议 / 增量 0** | ✅ |

> 51 条建议全部落在其他模块（琴生不等式、函数凸凹性表述），本专题 **0 命中**。

### 1.2 七轮整改落实度

- **P0：4 / 4 已闭环**（动点三类展开路径、线面页法向量、进阶球 SSOT、内切球切点门控）
- **P1：11 条完全闭环 + 3 条半闭环** → 本轮把 3 条半闭环**全部收口**（见 §二 #2 / #6 / #7）
- **本轮新发现：1 条 P0 + 4 条 P1**（全部已修，见 §二）

### 1.3 本轮新发现（危害排序）

| 编号 | 位置 | 现象 | 验算 |
|:---|:---|:---|:---|
| 🟠 **P0-新1** | `solidRotationBody.ts` `reasoningSteps` | 三步推导只按 `semicircle` / `rightTrapezoid` / **else** 三分支，而 `shape` 实有 4 值 ⇒ **圆柱（默认形状）落入 else，整套套用圆锥公式** | 默认 r=1.5、h=3：看板 S_侧=**28.27**、V=**21.21**；推导链给出 πrl=**15.81**、⅓πr²h=**7.07**、α=**160.99°**（圆柱展开是矩形，不存在圆心角） |
| 🟠 P1-新1 | `SphereCutSection.tsx:40` | `cutZ` 用未钳制的 `cutDistance`，`rCut` 却取自钳制后的 `cutInfo` ⇒ R=0.5、d=2.5 时"截面圆心 O₁"被画到球外 z=2.5，而右屏正显示"相离" | `calculateSphereCut` 内部钳制（`:145`），两边不同源 |
| 🟠 P1-新2 | `solidLinePlane.ts:115-116` | 几何法只写 `EF ∥ BC ⟹ EF ∥ 面 PAD`。BC 上各点 x=a，而面 PAD 即 x=0 ⇒ **BC ⊄ 面 PAD**，不满足判定定理要件 | 正确链：`EF ∥ BC ∥ AD`，`AD ⊂ 面 PAD`，`EF ⊄ 面 PAD` |
| 🟠 P1-新3 | `vectorBasis.ts:389` | latex 写「**不共线**判据：x+y=1」，方向反了（x+y=1 是**共线**判据），与同行 detail（`:387-388`，正确）自相矛盾 | 同文件 `:216` 亦写 `x+y=1 ⟺ 共线`，佐证方向 |
| 🟠 P1-新4 | `solidParametricPoint.ts:156` | λ=0 警示称"截面 PAC 退化为直线 AC"。实为 P 与 B 重合，A、B、C 三点不共线 ⇒ 面 PAC **就是底面**，二面角 θ=0° | 同页 `:151` 的 gaokaoPoint 写的正是 θ=0° |

**误报反证（1 条）**：子代理报「`LinePlaneRelationScene.tsx` 直角方框 `dirA={0,0,1}` 硬编码」——不可达：该标记只在 `perpendicular` 分支渲染，而切入该模式时强制 `thetaDeg: 90` 且该模式无可调参数 ⇒ θ 恒 90°，硬编码正确。

---

## 二、修复清单（本轮 17 处改动，全部已验证）

| # | 级别 | 文件:行（改后） | 修法 |
|:--|:---|:---|:---|
| 1 | **P0** | `src/data/builders/solidRotationBody.ts:454 / 471 / 486` | 三步推导各补 `shape === "rectangle"` 分支：step1 给 `母线 ∥ 轴 ⟹ l = h`；step2 给 `S_侧 = 2πrh、S_全 = 2πr(r+h)、V = πr²h`；step3 给"侧面展开为矩形，两边为 2πr 与 h，`L_min = √((2πr)²+h²)`" |
| 2 | P1 | `src/components/Math3D/solids/SphereCutSection.tsx:43-44, 74-124, 200-215` | 引入 `hasCut = \|d\| < R − 1e-6`：`rCut` 仅在相交态取 `cutInfo.cutRadius`；相切/相离态隐藏小圆面、圆周、r_截 边、斜边 R、A 点与 r/R 标签，并加提示「d ≥ R：相离，无公共点 / 相切，圆退化为一点」。O′ 与 d 标签仍按真实平面位置摆放（几何真实），但不再画出不存在的截面圆 |
| 3 | P1 | `src/data/builders/solidLinePlane.ts:115-116` | detail + latex 改为 `EF ∥ BC ∥ AD，AD ⊂ 面 PAD，EF ⊄ 面 PAD ⟹ EF ∥ 面 PAD`，并说明"BC 不在面内、须换面内中转直线" |
| 4 | P1 | `src/data/builders/vectorBasis.ts:389` | 改为 `A,B,P 共线 ⟺ x+y=1；x+y=当前值（=1 / ≠1）⟹ 共线 / 不共线` |
| 5 | P1 | `src/data/builders/solidParametricPoint.ts:158` | λ=0 警示改为「P 与 B 重合，面 PAC 与底面 ABCD 重合（并非退化为直线 AC），二面角 θ = 0°」 |
| 6 | P1（旧 P1-4 收口） | `src/data/builders/solidRotationBody.ts:251-259` | 圆锥 α>180° 新增警示：扇形不再是凸图形，弦会穿出锥面 ⇒ `L_min = 2l` 而非 `2l·sin(α/2)`，并把弦长同屏对照。math 层 `rotationProfiles.ts:190-191` 与 builder `:144-145` 早已有该分支，**本轮补的是教学解释** |
| 7 | P2 | `src/data/builders/vectorBasis.ts`（13 条 rubric） | 12 条旧格式「采分点：…（N分）」统一为项目格式「`[高考采分点] … (+N分)」；全专题 rubric 合规率由 119/131 → **131/131** |
| 8 | P2 | `src/data/builders/solidLinePlane.ts:180-181` | 面面平行判据由 `A₁/A₂ = B₁/B₂ = C₁/C₂`（n₂=(0,0,1) 时 0/0 无定义）改为 `∃ k ≠ 0 : n₁ = k·n₂`，condition 补比值式适用前提 |
| 9 | P2 | `src/data/builders/solidRotationBody.ts:341` | 「r₂=0 (**S₁**=0)」改为「r₂=0 (**S₂**=0)」（`S₂ = πr₂²`） |
| 10 | P2 | `src/data/builders/solidAdvancedSphere.ts:271` | 「**严格**满足内切圆充要条件」改为「在容差 \|h − 2√(r₁r₂)\| < 0.1 内可视为满足（**近似判定**）」，与 `advancedSphereModels.ts:227` 实际判据对齐 |
| 11 | P2 | `src/data/builders/solidAdvancedSphere.ts:265` | 圆台 r₁/r₂ 口径消歧：注明"本页 r₁=上底、r₂=下底，与「旋转体」页相反" |
| 12 | P2 | `src/data/builders/solidAdvancedSphere.ts:199`、`src/data/builders/solidPolyhedronSphere.ts:283` | 为教材正文未列的模型补**条目级拓展声明**：正四面体「棱切球」、对棱相等四面体「补形（汉堡）模型」各加 `isExtension: true` + `extensionBadge: "拓展 · 超出课标"`（专题内 `isExtension` 由 0 → 2 命中） |
| 13 | P2 | `src/components/Math3D/solids/CircumInSphereScene.tsx:294-299` | 内切球切点由统一 `T` 改为带下标 `T₁, T₂ …`（正方体 6 个切点同屏时可区分），`PointLabel3D` → `CompoundLabel3D` |
| 14 | P2 | `src/features/solidGeometry/CircumInSphereAnimation.tsx:431-437` | `cuboid_std`（3-4-12）预设在**内切球**模式下原仍显示外接球文案；改为「长方体一般无内切球」陷阱提示，给出 `r = min(a,b,h)/2 = 0.75` 的"内部最大相切球"说明 |
| 15 | P2 | `src/components/Math3D/solids/AdvancedSphereScene.tsx:506-540` | 三球同心模型补三条半径连线（O→顶点 = R、O→棱切点 = r_棱、O→面切点 = r_内），此前只有球壳与切点，1 : √3 : 3 的比例关系无法从图上读出 |
| 16 | 守护 | `src/test/rotationBodyShapeChain.test.ts`（新增） | 防 P0-新1 复发：断言默认 `shape="rectangle"` 的三步推导走圆柱公式（含 `2\pi r h`、`\pi r^2 h`，不含 `⅓πr²h`、`l = √(r²+h²)`、`α = r/l`），并回归保护圆锥仍走圆锥公式 |
| 17 | P2（口径） | `src/components/UI/mathPanel/MathReasoningSection.tsx:51-59` | 「高考破题三步推演链」标题下新增分值口径说明：「分步所列分值为**单问内的采分点拆分**，累加不等于整题分值」（仅当该组 `steps` 中存在 `rubric` 时显示）。消除首页徽标「15 题 · 13 分」与个别页面 rubric 累计（60 / 30 / 15 等客观题分制）之间的观感矛盾 —— 与本轮 P0 同属"同屏数字自相矛盾会削弱信任"的一类问题。<br/>**影响面**：该组件是全项目右屏共享组件，改动波及所有知识树模块而非仅本专题。之所以按全局一致口径处理：① 文案陈述的"步骤拆分 ≠ 整题分值"在所有模块同样成立（如 `triangleSolve` 的 1+2+3=6 也是单题内拆分）；② 若改为专题开关，需向 `MathPanelData` 加字段并在 12 个 builder + 组件链上逐处透传，成本远高于收益 |

**未改动的判定**：线面垂直页直角方框 `dirA` 硬编码（不可达，见 §一 1.3 误报反证）。

---

## 三、验证

1. **类型检查**：`tsc -b` → exit 0
2. **全量测试**：`vitest run` → **141 文件 / 1328 用例全通过**（含新增的 `rotationBodyShapeChain.test.ts`，2 用例）
3. **静态审计**：`npm run audit:strict` → 714 文件 / **0 错误 / 增量 0**（51 条建议均为其他模块存量）
3b. **UI 回归**：#17 改的是右屏共享组件，`vitest` 141 文件 / 1328 用例无快照或断言被破坏（`rightPanelVerification`、`solid3dRender.contract` 均仍通过）
4. **P0 修复实证**：新增守护测试直接调用 `buildRotationBodyPanel({shape:"rectangle", r1:1.5, height:3})`，断言推导链字面量；圆锥分支同测防回归 —— 2 用例通过

> 说明：`audit:strict` 通过 ≠ 内容正确。本轮 P0（圆柱套圆锥公式）在七轮里始终全绿通过门禁，是靠**逐条取实际取值验算**才暴露的。

---

## 四、旧 09-20 报告的过期陈述（已在原文件回改）

| # | 旧陈述 | 更正 |
|:--|:---|:---|
| 1 | 修复报告 §6.2：「3D 真渲染守护**仍空白**」 | **已过期**：`src/test/solid3dRender.contract.test.tsx`（11:18 新增，234 行）+ `src/test/harness/threeTestLayer.tsx` 已真渲染 12 页并做几何断言（球包住所有顶点、至少一个顶点恰在球面、几何属性无 NaN/Infinity） |
| 2 | 修复报告头部：「`npm run test` **132 文件 / 1270 用例**」 | 子集口径（其复核命令不含 `src/components/`），全量实测 **140 文件 / 1326 用例** |
| 3 | 四轮结论：「rubric **119/119 统一**」 | 不含 `vectorBasis.ts` 后加批次：本专题 131 条中 12 条旧格式，**本轮已统一为 131/131** |
| 4 | 修复报告 §0：「P1-4（圆锥 α>π）**待教材口径确认**」 | 已在 math 层与 builder 双层实现分支，本轮补教学警示 ⇒ **闭环** |

回改方式：在原报告头部加「后续更正」引用块，并在 §6.2 对应行末追加更正标记（保留原文，不删除历史陈述）。

---

## 五、遗留（未改动，需决策）

| # | 事项 | 现状与建议 |
|:--|:---|:---|
| 1 | **旋转体侧面展开图可视化** | `math3d/rotationProfiles.ts:163` 的 `UnfoldParamsResult`（母线长、展开圆心角、绕侧一周最短）已算好，但 `RotationBodyAnimation.tsx` / `RotationSweep.tsx` **无展开图渲染** ⇒ 页面讲了"化曲为直"却看不到展开图形。属**新增可视化功能**（非缺陷），需新增组件 + 版面排期，本轮未做 |
| 2 | 圆台 `r₁/r₂` 两页口径相反 | 本轮仅做文案消歧（#11）。统一语义需同时改 `solidAdvancedSphere` / `advancedSphereModels` / `AdvancedSphereScene` 或旋转体侧，牵动中屏，建议单独立项 |
| 3 | ~~分值口径多档~~ | 🟢 **已闭环（09-20 13:20，见 §二 #17）**：推演链区块已加"单问内的采分点拆分，累加不等于整题分值"说明。各页 rubric 仍是多档并存（13 / 15 / 26 / 39 / 52 / 60·30·15），但这是**客观题小题分制与解答单问拆分的客观差异**，已不再表现为矛盾 |
| 4 | 3D 真渲染守护覆盖面 | 现仅覆盖**默认参数**，几何断言仅覆盖外接球 3 页，其余 9 页为"结构 + 数值有限"守护。建议按参数域极值补针对性用例（λ=0/1、α→0/180°、d=±R、r₂=r₁） |

---

## 附录：本轮验算无误（避免误伤）

- **动点三类路径**：a=2,b=5,c=1 ⇒ path1=√50=7.071、path2=√40=6.325、path3=√34=**5.831**，`globalMinLength` 取三者最小 ✓
- **内切球切点**：默认 a=3,b=2,c=2 ⇒ r=1，只生成 4 个真切点（tBottom/tTop/tRight/tLeft），`|center − t| = 1 = r` ✓
- **二面角平面角**：棱 BD、M 为垂足、∠AME ✓
- **圆台球心距**：`d = (h² + r₁² − r₂²)/(2h)` 与 `R² = r₂² + d²` 联立自洽（下底在 Z=0，半径 r₂）✓
- **距离页 λ=0.1**：右屏 / 中屏 / 标签三处同源 ✓
- **球截面右屏三态**：`|d|<R` 相交 / `=R` 相切 / `>R` 相离 ✓
