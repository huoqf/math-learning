# math-learning 高中数学项目审查报告

> 审查对象：`D:\code\math\math-learning`（MathVision 高中数学交互学习系统，React 19 + TS 5.7 + Vite 6 + Electron）
> 审查时间：2026-09-13
> 审查方式：只读静态审查（未修改任何代码/配置/文档）
> 审查范围：① 现有测试与门禁体系；② 全部 72 个页面（10 章）的数学内容
> 参照标准：人教A版 2019 新课标教材（必修第一/二册、选择性必修第一/二/三册）+ 新高考 2024–2026 命题结构

---

## 0. 结论摘要

| 维度 | 结论 |
|:---|:---|
| **门禁有效性** | ⚠️ **门禁形同虚设**。`npm run audit:strict` 实际只扫 `src/features`（502 文件 / 0 违规 / exit 0）；而 `src/data`（140 文件 / **556 处 error**）与 `src/components`（91 文件 / 2 处 error）**从未被扫描**。CI、husky 都跑的是这条命令。 |
| **门禁与"高中数学"的匹配度** | ❌ **零匹配**。30 条规则全部是「色彩 Token / 字号缩放 / 三屏联动 / LaTeX 定界符 / 推导链格式」类工程规范，**没有一条校验是否超纲、是否选学、教材册次归属、数学结论正确性**；全量测试里也没有任何「超纲 / 选学 / extend」断言。 |
| **测试有效性** | ✅ 工程质量不错：109 文件 / **1015 用例全绿**（21.9s），其中三屏数值契约、定义域安全巡检、KaTeX 语法校验、56 页冒烟渲染都是实打实的。但它保证的是"**能跑、不崩、渲染得出来**"，不保证"**内容对、不超纲**"。 |
| **内容合规性** | ❌ **发现 33 类问题**，其中 **P0 级 5 项**（含 2 处会直接误导学生的数学错误）、**P1 级 12 项**（超纲内容被当"高考核心/压轴"且全库无一处 `extend` 标注） |

**一句话总结**：这个项目的工程纪律（色彩、字号、布局、三屏联动）做得相当扎实，但**教学纪律（学段边界、课标范围）目前完全依赖人工自觉，机器一点都没管**。P1 文档里"全库 365 文件 0 违规"的结论是范围缩水后的假象，不是真实的全库状态。

---

## 第一部分：测试与门禁审查

### 1.1 现有门禁盘点

| 门禁 | 入口 | 实际命令 | 覆盖范围 | 实测结果 |
|:---|:---|:---|:---|:---|
| 静态规范审计 | `package.json:16-17` | `audit_page.mjs` / `--strict` | **仅 `src/features`**（见 1.2） | ✅ 502 文件 / 0 违规 / exit 0 |
| 类型检查 | `.husky/pre-commit:3`、`.github/workflows/ci.yml:21` | `tsc -b` / `tsc --noEmit -p tsconfig.json` | `include: ["src", ".agents"]` | ✅ exit 0 |
| ESLint | `package.json:15`、`ci.yml:20` | `eslint src/ ...` | 全 `src` | 未见阻断（见 1.3） |
| 单元测试 | `ci.yml:35` | `npm run test:ci` = `vitest run` | `src/**/*.test.{ts,tsx}` | ✅ 109 文件 / 1015 用例全过 |
| E2E | `playwright.config.ts` | `playwright test` | `e2e/formula-render.spec.ts`（仅 1 个 spec） | ❌ **CI 中未接入** |
| 提交前 | `.husky/pre-commit` | `lint-staged` + `audit:strict` + `tsc -b` | — | ⚠️ 不含单测；`tsc -b` 命中 tsbuildinfo 缓存会空跑 |

### 1.2 【P0】审计门禁覆盖面严重缩水 —— 假性"零违规"

`audit_page.mjs` 默认目标目录为 `src/features`：

```js
// audit_page.mjs:66
const targetArg = args.find((arg) => !arg.startsWith('-')) || 'src/features';
```

`runner.mjs:39-42` 仅在目标**恰好等于** `src/features` 或 `''` 时，才额外补扫 `src/math`、`src/math3d`：

```js
const targetRel = path.relative(workspaceRoot, targetDir).replace(/\\/g, '/');
if (targetRel === 'src/features' || targetRel === '') {
  walkDirectory(path.resolve(workspaceRoot, 'src/math'), files);
  walkDirectory(path.resolve(workspaceRoot, 'src/math3d'), files);
}
```

后果实测（均以 `--strict` 运行）：

| 扫描目标 | 文件数 | error | exit |
|:---|---:|---:|---:|
| `src/features`（= 门禁默认） | 502 | **0** | 0 |
| `src/math` | 88 | 0 | 0 |
| `src/math3d` | 46 | 0 | 0 |
| `src/components` | 91 | **2** | **1（拦截）** |
| `src/data` | 140 | **556** | **1（拦截）** |
| `src`（全库） | 778 | **560** | **1（拦截）** |

`src/data` 的 556 处 error 分布：

| 规则 ID | 处数 | 类型 |
|:---|---:|:---|
| `left/param-label-format` | 347 | 参数未绑定色彩 Token (242) / 孤立参数代号 (88) / 参数缺少数学代号 (17) |
| `discipline/text-delimiter` | 177 | 混合文本缺少 `$` 定界符 |
| `discipline/raw-latex-instructions` | 15 | 文本缺少 `$` 定界符 |
| `right/algebra-rigor` | 9 | 代数式未省略系数 1 等 |
| `left/param-registry-meta` | 6 | 参数过多未分组 / 动参数动线倒挂 |
| 其余 | 3 | 孤立数值跳步 / TipCard 剧透 |

**关键点：`src/data` 不是死代码。** `features/*/Animation.tsx` 普遍 `import { paramMeta, defaultParams } from "@/data/registries/xxx"`（如 `registries/sequence.ts` 被 5 个页面引用、`registries/constant.ts` 被 4 个页面引用），这些文件直接驱动左屏参数面板；`src/data/builders/*` 直接生产右屏 `MathPanelData`。也就是说：**没被门禁扫到的目录，恰好是承载教学内容的主体**。

另两处漏扫：
- `src/components/Math/SceneLabelGroup.tsx:43` → `stroke="#ffffff"`（硬编码 Hex）
- `src/components/Math3D/FormulaLabel3D.tsx:78` → `background: "rgba(255,255,255,0.92)"`

两者在 `audit src/components --strict` 下均被正确判定为 ERROR 并 exit 1，说明**规则本身是有效的，是"跑错了范围"**。

### 1.3 【P1】文档与规范宣称不一致

| 位置 | 原文 | 实际 |
|:---|:---|:---|
| `AGENTS.md:76` | 「`npm run audit:strict` **全库**违规数必须为 0，任何存量/增量违规直接非零退出阻断构建」 | 只覆盖 `src/features`；`src` 全库为 560 处 error |
| `docs/agent-rules/process/TODO_deferred.md:4` | 「P1 存量规范治理圆满收官（**全库 365 文件**严格审计 0 违规，清除率 100%）」 | 同一文件 `:23` 自己写的是 `audit:strict src/features`，口径前后不一 |
| `AGENTS.md:71` | 「严禁源码中直接出现 `#` 十六进制色值或 `rgb()`」 | `style/no-hardcoded-hex` 只匹配 `fill="#` / `stroke="#`，故 `\color{#DC2626}{...}` 这类硬编码六角色**不被拦截**（见 `registries/sequence.ts:188,212,219,288,311`） |

ESLint 侧：`package.json:15` 的 `lint` 带 `--max-warnings 0`，但 `ci.yml:20` 用的是 `npx eslint src/ --report-unused-disable-directives`（**无 `--max-warnings`**）；而 `eslint.config.mjs:33-36` 把 `no-explicit-any`、`no-unused-vars`、`react-refresh/only-export-components` 都设为 `warn`。→ CI 的 lint 环节基本不会拦下任何东西。

### 1.4 【P1】规则适用范围过宽 → 大量误报，掩盖真实问题

`left/param-label-format`（`left-panel.mjs:236-275`）判定的是"参数标签必须含中文含义 + 色彩 Token"，但它命中的 347 处里绝大多数其实是**滑块刻度 `marks[].labelFormula`**（取值本身，如 `"0"` / `"1"` / `"r=2"`），并非参数标签。例：

```
src/data/registries/sequence.ts:164  { value: -3, label: "负首项 (-3)", labelFormula: "-3" },
src/data/registries/sequence.ts:252  { value: 0, label: "0 (展开)", labelFormula: "0" },
```

规则没有区分 `ParamMeta.labelFormula` 与 `ParamMark.labelFormula`（两者在 `types.ts:73,80` 是不同字段）。这类噪音会让"全库 556 处"这个数字既吓人又不可直接用于整改。

**但其中确实混有真问题**，例如同文件：
- `registries/sequence.ts:244` → `labelFormula: "\\lambda"`（参数标签只有孤立代号，无中文含义、无色彩）
- `registries/sequence.ts:260` → `labelFormula: "N"`
- `registries/sequence.ts:92` → `desc: "a_{n+1}-a_n=2n，累加得 a_n=n^2-n+1"`（中文句内 LaTeX 未用 `$` 包裹，会裸露源码）

### 1.5 【P1】门禁完全不校验数学正确性与学段边界（核心缺口）

30 条规则的分布：

| 规则组 | 条数 | 校验内容 |
|:---|---:|:---|
| `architecture` | 4 | 数学层禁 React/DOM、禁 BrowserRouter、滚动容器 |
| `left-panel` | 9 | TipCard 存在/动线/设问词库、参数分组、标签色彩、SelectGrid 纯净 |
| `center-canvas` | 6 | 禁裸浮点文字、禁 SVG 裸 LaTeX、字号缩放、拖拽二次转换、3D 范式纯净、数列离散 |
| `right-panel` | 4 | 模式上下文透传、Builder 模式隔离、代数式规范、推导链跳步 |
| `style-tokens` | 3 | 硬编码 Hex/RGB、JSX 双斜杠 |
| `discipline` | 4 | 向量符号、nCr 记号、`\perp`、物理单位、`$` 定界符 |

**结论：0 条**涉及"是否超出课标范围""是否为选学/拓展内容""教材册次归属是否正确""数学结论是否正确"。

同样的缺口在测试层：对 `src/test` 全目录检索 `超纲|选学|拓展|extend` —— **零命中**。

而项目类型系统里其实**预留了机制却没启用**：

```ts
// src/data/types.ts:44
importance: "basic" | "core" | "gaokao" | "hard" | "extend";
```

全库 `importance: "extend"` 只出现 **1 次**（`src/data/builders/complex.ts:216`）。`KnowledgeNode`（`types.ts:39-64`）也**没有"教材册次/年级"字段**，所以"这页属于必修几"在法律上无法被机器表达，只能靠 `badge` 文案人工手写（且只写了约 20 处，覆盖不到 72 个节点）。

### 1.6 门禁在"符合高中数学"上的真实水位

**做得对的**（可保留、值得肯定）：
- `src/test/syncContract.test.ts`：20 个专题级三屏数值契约，用**手算真值**（如墙角外接球 R=2.5√2、抛物线焦点弦 |AB|=2p=4、贝叶斯后验 27.94%、等比 S₄=80）做 `toBeCloseTo` 断言，并校验公式符号（`expectedReasoningSymbols`）与跨模式隔离（`forbiddenTheoremKeywords`）。这是**唯一带数学语义的测试**，方向完全正确。
- `src/test/presetParamsDomainSafety.test.ts`：94 用例，逐一巡检 72+ 页的 `NaN/Infinity` 与临界退化参数稳健性。
- `src/test/katexSyntaxValidation.test.ts`：22 用例，全库 LaTeX 离线预编译校验。
- `src/test/corePagesSmoke.test.tsx`：56 页挂载冒烟。
- `src/test/autoRegistryFuzz.test.ts`：38 用例，registry 参数组合模糊测试。

**缺口**：这些测试的断言全部是"不崩""数值等于某某""关键词在/不在"，没有任何一条断言"这个页面的内容属于高中数学"。

**可复用的既有资产**：`syncContract.test.ts:568-573,628,653` 已经在用 `forbiddenTheoremKeywords` 做黑名单（如禁止在二面角页出现"异面直线公垂线""点到平面的距离公式"）。**同一机制把关键词换成「洛必达 / 麦克劳林 / 泰勒 / 琴生 / 凹凸 / 拐点 / 行列式 / 克拉默 / 外积 / 极坐标 / 参数方程 / 无穷级数 / 马尔可夫 / 卡方分布 / 概率密度 / 上确界 / 夹逼 / 等价无穷小 / 特征方程 / 端点效应 / 极点极线」即可直接变成"超纲词门禁"**——这是成本最低的补救路径。

---

## 第二部分：逐页内容审查（按章节）

> 覆盖 `routeEntries.ts` 中全部 72 个节点 / 10 章。
> 分三级：**P0 数学错误或严重误导** → **P1 超纲/超学段** → **P2 严谨性与规范**。

### 2.1 【P0】数学错误 —— 会直接教错学生

#### C1. 三角函数线页：第四象限符号全错

`src/data/builders/trigLines.ts:85-94`

```js
const qMap = {
  1: "第一象限 (sin>0, cos>0, tan>0)",
  2: "第二象限 (sin>0, cos<0, tan<0)",
  3: "第三象限 (sin<0, cos<0, tan>0)",
  4: "第四象限 (sin<0, cos<0, tan<0)",   // ← cos 应为 >0
  ...
};
```

该字符串在 `:96` 取出，并在 `:143` 拼入口述记忆：

```js
const mnemonic = `当前位置：${quadrantText}。正弦看竖线(MP)，余弦看横线(OM)，正切看右切线(AT)。…`;
```

随 `MathPanelData.mnemonic` 上屏。学生把角拖到第四象限（如 300°）时，看到的是"cos<0"，与余弦线 OM 正向的图形**直接矛盾**。这类"图形对了、文字反了"的错误比纯代码 bug 更有害。

#### C2. 对数二阶放缩不等式链是假命题

`src/data/builders/transcendental.ts:284-295`

```js
currentModeTheorems.push({
  name: "对数二次上界放缩不等式",
  latex: `\\ln x \\le \\frac{x^2 - 1}{2} \\le x - 1 \\quad (x > 0)`,
  level: "core",
  prerequisites: [
    "利用切线进一步构造二次抛物线上界",
    "在 x > 1 时比线性切线更贴合对数曲线",   // ← 与该不等式链自相矛盾
    ...
```

- 前半段 `ln x ≤ (x²-1)/2` 成立（令 g(x)=(x²-1)/2-ln x，g'(x)=(x²-1)/x，x=1 取极小值 0）。✅
- 后半段 `(x²-1)/2 ≤ x-1` 移项即 `(x-1)² ≤ 0`，**除 x=1 外恒不成立**（x=3 时 4 ≰ 2）。❌
- `prerequisites` 里"在 x>1 时比线性切线更贴合"更进一步说明作者以为两者可比较——而 x>1 时 `(x²-1)/2` 恒大于 `x-1`。

同一文件 `:520` 附近有同源的错误断言。该定理 `level: "core"`，会作为右屏核心定理展示。

#### C3. 数列页把 S∞ 与数列极限作为正文结论（双重问题：既算错语义又超纲）

- `src/math/sequence.ts:437-441`：`if (Math.abs(q) < 1) limitSum = a1 / (1 - q);`
- `src/data/builders/sequence.ts:391`：定理名「无穷递缩等比数列求和定理」，latex 写 `S_\infty = \lim_{n\to\infty} S_n = a_1/(1-q)`
- `:396` gaokaoPoints：「极限思想…收敛」
- `:514-517 / :543-547 / :700-707`：`\lim T_N = 0.7500`、`极限收敛值 1.0000`、`极限稳态 a_\infty`
- 上屏场景：`SequenceGeometricExponentialScene.tsx:72,92`、`SequenceGeometricTessellationScene.tsx:190`、`GeometricPage.tsx:193`

人教A版选择性必修第二册第四章仅含"数列概念 / 等差数列 / 等比数列 / 数学归纳法"，**不含数列极限，不含无穷级数求和**（`S∞=a₁/(1−q)` 属旧大纲内容）。这是全库最典型的"用旧大纲当新高考"。

#### C4. 等差数列 |aₙ| 前 n 项和的性质说反了

`src/features/sequence/ArithmeticPage.tsx:169`

> 「｜aₙ｜前 n 项和 Tₙ 的折线在变号项处为何出现**极小值尖点**？」

`Tₙ = Σ|aₖ|` 单调不减（每项非负），**不存在极小值**。变号点处的特征是"**折线斜率（增量 |aₙ|）取最小**"，即拐点而非极小点。同一页 `:152` 写「Sₙ 关于 n 是开口向上的二次函数」也未区分 d<0 的情形。

#### C5. 主辐角加法式不成立

`src/data/builders/complex.ts:190`：「辐角相加：`\arg(z_1z_2)=\theta_1+\theta_2`」，而 `src/math/complex.ts:34-40` 的 `argument` 实现的是**主辐角 ∈ (-π, π]**。主辐角不满足无条件加法，必须注明"允许相差 2kπ"。

---

### 2.2 【P1】超纲 / 超学段内容（按章汇总）

> 统一症状：**该标注为"拓展/选学/强基"的内容，被写成"高考核心/高考压轴/母体源头"，且 `importance` 填 `"hard"` 而非 `"extend"`。**

#### 第 7 章 导数及其应用 —— 最严重

| 问题 | 证据 |
|:---|:---|
| **洛必达法则当"高考核心"** | `derivative-endpoint-taylor/meta.ts:5-9`：`title: "端点效应与洛必达/泰勒拟合放缩"`、`chapter: "导数及其应用"`、`importance: "hard"`；`knowledgeTree/derivative.ts:111-116` 同，且 `examWeight: 5` |
| 泰勒/麦克劳林当"命题母体" | `data/builders/derivativeEndpointTaylor.ts:136-139`（定理名「草稿纸探路神器：洛必达法则 (L'Hôpital's Rule)」+ 连等求导）；`:193-195`「麦克劳林多项式 $P_n(x)$」作为右屏 quantity；`:216-218`「新高考放缩不等式母体源头」；`:228`「掌握泰勒展开可秒懂命题人意图」 |
| 上屏设问直接教洛必达 | `DerivativeEndpointTaylorAnimation.tsx:196-200`：「草稿纸如何用洛必达法则快速锁定参数临界？」 |
| 二阶导判凹凸/拐点/琴生 | `SecondDerivativeAnimation.tsx:285`（「高考大招·三次曲线凹凸性与切线放缩」）、`:347`（「高考压轴·割线中点与弧中点（琴生不等式）」）、`:357,361`（极值点偏移）、`:389`（模式名「琴生不等式」）；`data/builders/secondDerivative.ts:173-222,262` |
| 定积分混入 | `data/builders/tangentScaling.ts:1000`（「进行定积分面积包围估计」）；`data/builders/solidRotationBody.ts:410,431`（"球体由无数薄壳微元积分累加"） |
| 端点效应当通法 | `data/builders/constantSingle.ts:482,518`；`math/derivativeEndpointTaylor.ts:189` 注释直言"紧扣新高考核心" |

#### 第 9 章 立体几何与空间向量

| 问题 | 证据 |
|:---|:---|
| **球坐标参数暴露为调节参数**（`discipline-specs.md:53` 明文禁止） | `registries/solidGeometry.ts:182-190`（"旋转方位角 φ" 0–360°，`importance: core`）；`:271-297`（"倾斜角 θ"、"截面方位角 φ"）；实现层 `math3d/lineRelation.ts:36-46`、`math3d/surfaceRelation.ts:74-93` |
| 叉积 / 外积当"大题首选" | `data/builders/solidSpatialDistance.ts:49,176-178,205-210,237`（`\vec{n}=\vec{u}\times\vec{v}`、「向量外积法与…」、「设法向量 n=u×v，大题首选」）；`solidSpatialAngle.ts:110` |
| 平面方程一般式当定理 | `data/builders/solidSurfaceRelation.ts:156-159`（`d=|D₁-D₂|/√(A²+B²+C²)`，note 明说基于 `Ax+By+Cz+D=0`） |
| 教材无绝对值不等式却标"课标基础" | `inequalityAbsolute/meta.ts:14`、`scenarios.ts:25`（"课标基础 · 数轴距离模型"）；`data/builders/inequalityAbsolute.ts:395`（"柯西不等式"、"高考选考"——均为已退出课标的表述） |

#### 第 8 章 平面解析几何

| 问题 | 证据 |
|:---|:---|
| **直线参数 t 方程当正文**（《坐标系与参数方程》选讲已于 2019 人教A版取消） | `conicParamT/meta.ts:5-9`（`importance: "hard"`、`module: "圆锥曲线压轴"`）；`knowledgeTree/conic.ts:126-139`；`LineParamTAnimation.tsx:324-381` 徽标「参数 t 的几何意义本质 / 高考模型·中点弦条件」 |
| 同上（另一页） | `conicParam/meta.ts:5`、`ConicParamAnimation.tsx:224,243`（"高考经典·直线标准参数方程与模长"） |
| 齐次化当"高考压轴" | `conicHomogenization/meta.ts:5-9`；`ConicHomogenizationAnimation.tsx:250,269` |
| 阿基米德三角形当"新高考核心" | `parabolaArchimedes/meta.ts:5-9`；`ParabolaArchimedesAnimation.tsx:258,267,275` |
| 极点极线术语进入推导链 | `data/builders/lineCircle.ts:228,291-294`；`data/builders/conicLine.ts:409,426,559`（"高考自招拔高·极点极线与切点弦对偶定理"） |
| 圆锥曲线第二定义未标拓展 | `math/conicDefinition.ts:239`；`data/builders/conicDefinition.ts:395`；`conicProperties/math` 椭圆准线 `x=±a²/c` |

#### 第 6 章 数列

| 问题 | 证据 |
|:---|:---|
| 特征方程法当"高考进阶" | `knowledgeTree/sequence.ts:62`（`examMethod: "不动点特征方程与待定系数构造等比数列"`）；`data/builders/sequence.ts:919-920,938`（二阶线性递推通项 `a_n=C₁r₁ⁿ+C₂r₂ⁿ`、「二阶递推标准解法」）；`RecurrencePage.tsx:361-368`（badge「高考进阶 · 二阶特征根法」）；`SequenceAnimation.tsx:520-523` |
| S∞（见 C3） | — |

#### 第 5 章 平面向量与复数

| 问题 | 证据 |
|:---|:---|
| 行列式 / 克拉默法则 / 外积命名 | `math/vectorBasis.ts:74-78`（`crossProduct2D`）、`:113`（"2D 行列式 (外积) D"）、`:121`（"克拉默法则求解"）；`data/builders/vectorBasis.ts:80,251`（gaokaoPoints「克拉默消元法则」）；`math/vectorLinear.ts:30,118`（`detAB`） |
| 复数指数形式 e^{iθ} | `data/builders/complex.ts:188`；`ComplexAnimation.tsx:406,532` |
| 复数三角形式标 core | `data/builders/complex.ts:191`（`level: "core"`）；`ComplexAnimation.tsx:444`；`meta.ts:9`、`knowledgeTree/vectorComplex.ts:56` |
| 阿波罗尼斯圆归错模块 + 未标拓展 | `vectorPolarizationApollonius/meta.ts:13-15`（`chapter: "解析几何"`、`module: "圆锥曲线压轴"`、`importance: "hard"`），而极化恒等式属平面向量拓展 |
| 奔驰定理未标拓展 | `data/builders/vectorLinear.ts:220`、`vectorBasis.ts:284` |

#### 第 10 章 概率与统计

| 问题 | 证据 |
|:---|:---|
| 超几何分布**方差**当 core | `data/builders/probabilityDistribution.ts:248-251`（`D(X)=np(1-p)(N-n)/(N-1)`）；`math/probabilityDistribution.ts:137`；`modeConfig.ts:311` 设问求方差 |
| 概率密度积分 + 中心极限定理 | `data/builders/probabilityNormal.ts:161-172`（`∫_{-∞}^{+∞}f(x)dx=1`、命名"中心极限定理"、`level: core`） |
| 卡方分布密度函数 | `pairedData/components/ChiSquarePlot.tsx:189`（`f(x)=(2πx)^(-1/2)e^(-x/2)`）；`math/pairedData.ts:575-583` |
| 偏态正态密度 + Simpson 积分 | `math/probabilityNormal.ts:44-63,149`；`registries/probabilityNormal.ts:95`（"偏态系数 α"） |
| "马尔可夫链"命名与定位 | `knowledgeTree/probability.ts:112-116`、`probabilityBayes/meta.ts:15-25`、`modeConfig.ts:197-235`（badge「高考压轴·马尔可夫链」） |
| 贝叶斯未标"选学" | `data/builders/probabilityBayes.ts:426-428,449`（"新高考通法·全概与贝叶斯求解3步法"） |
| Yates 连续性修正标 core | `math/pairedData.ts:523-526`、`data/builders/pairedData.ts:322-325` |

#### 第 3 章 函数概念与性质 —— 必修一页混入选修内容

| 问题 | 证据 |
|:---|:---|
| 大学术语"上确界/下确界/紧致" | `funcProperties/DomainPage.tsx:129`（「求解正弦函数的**上确界与下确界**，并确定**紧致**闭区间值域」） |
| 极限 / 渐近线语言 | `DomainPage.tsx:121`；`ExponentialPage.tsx:151,159,161`；`PowerPage.tsx:253`；`data/builders/funcComposite.ts:86`（定理里直接写 `\lim_{x \to x_0^-}`）；`math/function.ts:678` |
| 凹凸 / 可导 / 切线放缩前置到必修一 | `PowerPage.tsx:228,236`（"凹凸加速增长"、"原点可导性"）；`LogarithmicPage.tsx:161,171`；`TransformAnimation.tsx:138,162` |
| 超越临界值 `a_c=e^{1/e}` 上屏 | `ExponentialPage.tsx:84,91`、`LogarithmicPage.tsx:161` |
| 幂函数自由指数超出 5 个基准 | `registries/funcExpLog.ts:39-47`（α ∈ [-2, 3] step 0.5，含 α=0、-1.5、2.5）；`PowerPage.tsx:210-255` |

#### 第 4 章 三角函数

| 问题 | 证据 |
|:---|:---|
| **重要极限 + 夹逼准则当定理** | `data/builders/trigLines.ts:202-208`（`\lim_{x\to0^+}\frac{\sin x}{x}=1`，`condition: "夹逼准则"`，note 称"正弦函数导数证明的第一原初几何依据"） |
| "等价无穷小"术语 | 同文件 `:224-226` |
| cot 记号 | `trigIdentity/math/trigIdentity.ts:368,490`（课标只用 `1/tanα`） |
| 海伦公式标 important | `data/builders/triangleSolve.ts:474-479`（应标拓展） |
| ω<0 未声明 | `trigTransform/math/trigTransform.ts:127-128,140,170-175` 全程 `absOmega`，结论仅对 ω>0 成立 |

#### 第 1–2 章 集合与不等式

| 问题 | 证据 |
|:---|:---|
| ∀x₁∃x₂ 嵌套量词置于必修一 | `math/quantifiers.ts:11-13`、`data/builders/quantifiersBuilder.ts:82-85` |
| 绝对值不等式标"课标基础" | 见第 9 章表内条目 |

---

### 2.3 【P2】严谨性与规范问题

#### 前提条件缺失 / 表述不严谨

| # | 位置 | 问题 |
|:---|:---|:---|
| C6 | `data/builders/set.ts:84` | `prerequisites: ["A 与 B 为非空集合"]` —— `p⇒q ⟺ A⊆B` 对 A=∅ 同样成立（空集是任何集合子集），且与本项目 `math/set.ts:88` 自相矛盾 |
| C7 | `math/function.ts:749`、`FuncZeroAnimation.tsx:143` | 二分法只要求 `f(a)·f(b)<0`，**缺"在 [a,b] 上连续"**前提 |
| C8 | `data/builders/vectorDotProduct.ts:202-204` | 数量积定义未显式声明"对非零向量" |
| C9 | `data/builders/inequalityBasic.ts:195` | 括注写成 `(a+b\ge2\sqrt{ab})`（等价重述），未写核心条件 `a>0, b>0` |
| C10 | `data/builders/sequence.ts:331` | 等比片段和前提写 `(q^k \neq -1, S_k \neq 0)`，真正必要的是 `S_k≠0`（即 `q^k≠1`），疑似笔误 |
| C11 | `data/builders/vectorLinear.ts:220` 等 | 零向量约定与坐标共线判据并存时未统一说明 |

#### 参数安全契约违规（`discipline-specs.md:101-115` 明文要求）

| # | 位置 | 问题 |
|:---|:---|:---|
| C12 | `registries/conicHomogenization.ts:20-44` + `math/conicHomogenization.ts:88-168` | a∈[1,4]、b∈[1,3] 且**无 a>b 校验** → 可滑出非法椭圆（对照正确写法：`registries/conicProperties.ts:51-53` 有 `safeB = safeA - 0.01`） |
| C13 | `registries/lineParamT.ts:106-129` + `math/lineParamT.ts:123-131` | 同上，a∈[1.5,5]、b∈[1,4] 无钳制 |
| C14 | `registries/solidGeometry.ts:615-645` | 翻折角 α ∈ [0,180]（含端点），规范要求 ∈ (0°,180°)；`solidFolding.ts:311-315` 仅在端点告警 |
| C15 | `registries/solidGeometry.ts:648-679` + `math3d/folding.ts:49` | a∈[2,5]、b∈[1,4] 可 a<b，而 `lenED = a-b` 会变负 → 几何崩坏 |
| C16 | `registries/quadratic.ts:15-29` + `QuadraticAnimation.tsx:233` | a 含 0（标"退化为直线"），但仍展示 `x=-b/2a` |
| C17 | `registries/funcExpLog.ts:30-38` | 允许 a=1（仅提示退化），课标要求 a>0 且 a≠1 |
| C18 | `registries/inequalityBasic.ts:30,49` | 刻度 `value:2.0` 标「a=b (取等)」，仅在默认值下成立，拖动后标签失真 |

#### 数学定义域 / 几何量范围

| # | 位置 | 问题 |
|:---|:---|:---|
| C19 | `math3d/plane.ts:24-30`、`math3d/spatialAngle.ts:200-203` | 二面角实现恒取 `Math.abs`（"取锐角"），与二面角 [0°,180°] 不符 |
| C20 | `math/conicHomogenization.ts:228`、`LineParamTAnimation.tsx:236-251` | `toFixed(2)` 直接进 LaTeX → 上屏 `1.00 k^2`（对照正确写法：`ConicParamAnimation.tsx:204` 用正则去尾零） |
| C21 | `math/sequence.ts:1115`、`data/builders/sequence.ts:697,904,914,921,928` | 同类浮点尾零 + 未化简系数（`(-2.00)`） |

#### 设问质量（违反 `discipline-specs.md:118-127` 设问红线）

| # | 位置 | 问题 |
|:---|:---|:---|
| C22 | `SingleVarPage.tsx:69-71` | 设问括号内直接给出结论 `（a \le f(x)_{min}）` —— 剧透 |
| C23 | `VectorPolarizationApolloniusAnimation.tsx:437,447` | condition 直接给出极化恒等式，question 给出完整解法 |
| C24 | `SetAnimation.tsx:168-179`、`SetVennPage.tsx:121-125` | 「观察交、并、补、差的区域覆盖与样本点 P 的归属变化」「拖拽…观察」——空泛 |
| C25 | `CompositeAnimation.tsx:243-245` | 「拖动自变量动点 x，观察中间量…」——空泛 |
| C26 | `modeConfig.ts:319-320,351-352` | 「探究…极限收敛关系」空泛；另一条 question 是操作说明（「可在中屏画布…拖拽柱顶调节概率」）而非数学设问 |
| C27 | `RecurrencePage.tsx:376,383,398` | 提前剧透 `aₙ = a₁ + Σf(k)`；「观察参数对图像与通项结果的影响」空泛 |
| C28 | `TrigLinesAnimation.tsx:273` | 「改变锐角 x，观察三者面积比值如何逼近极限值 1？」 |

#### 元数据 / 归属错乱

| # | 位置 | 问题 |
|:---|:---|:---|
| C29 | `knowledgeTree/setLogic.ts:15,30,45` | 三个集合节点的 `gaokaoTopic` **全部填 `"func_derivative"`**，与内容无关 |
| C30 | `knowledgeTree/setLogic.ts:17` | `examMethod: "Venn 图容斥原理与数轴穿线法"` —— 穿线法属高次不等式通法，与 Venn 图不适配 |
| C31 | `nike/meta.ts:16,28,40` | `chapter: "函数概念与性质"`，但内容是均值/基本不等式（人教A版必修一第二章 2.2），且未建 `know-ineq-basic` 先修 |
| C32 | `quadratic/meta.ts:7`、`knowledgeTree/function.ts:54` | 二次函数与一元二次不等式挂"函数概念与性质"，教材为必修一第二章 2.3 |
| C33 | `knowledgeTree/trig.ts:83-108` | 解三角形（必修二第六章）与三角函数（必修一第五章）同挂 `chapter: "三角函数"`，未区分册次 |
| C34 | `data/builders/vectorDotProduct.ts:238` 等 | 大量 `badge` 手写册次（"必修二""选择性必修一"），覆盖仅约 20 处 / 72 节点，其余无册次信息 |

#### 其他

| # | 位置 | 问题 |
|:---|:---|:---|
| C35 | `math/set.ts:136` | 用「A∩B≠∅ 且有部分相异」表达"既不充分也不必要"，充要判据应为 `A⊄B ∧ B⊄A` |
| C36 | `funcProperties/SymmetryPage.tsx:230` vs `components/PropertiesScene.tsx:57` | 选项公式标 `y=(x-a)²`，实际绘制 `0.5(x-a)²-1.5`，不一致 |
| C37 | `math/transform.ts:86-89,143` | `foldMode="input"` 且 `h≠0` 时生成 `f(|x|-h)`，非教材标准式 `f(|x-h|)` |
| C38 | `data/builders/inequalityAbsolute.ts:324,348` | 「同号(`ab≥0`)」—— `ab≥0` 含 0，0 并非"同号" |
| C39 | `StandardPage.tsx:213`、`AmgmPage.tsx:180` | 「验证…的**充要条件**」应为"等号成立条件（当且仅当）" |
| C40 | `data/builders/derivativeMonotonicity.ts:311` | 术语命名"费马定理"（内容在课标内，命名超纲） |
| C41 | `data/builders/solidAdvancedSphere.ts:320` | 「高考微积分实际应用常考模型」措辞与立体几何定位不符 |
| C42 | `data/builders/pairedData.ts:304`、`math/pairedData.ts:537` | 「χ²=0…**完全接受**独立零假设」「接受无关联原假设」—— 严谨表述应为"没有充分证据推断两变量有关联" |
| C43 | `math/probabilityDistribution.ts:177-185,210`、`modeConfig.ts:92-100` | 学生输入 Σp≠1 时被静默归一化（`sumP` 硬编码 1.0），掩盖"概率之和为 1"这一教学要点 |
| C44 | `data/builders/statPercentile.ts:192-203`、`math/statPercentile.ts:118-141` | 标"百分位数线性插值公式 (**新课标标准**)"，但新课标定义是在**有序原始数据**上按 `i = np%` 是否整数取值；页面只做了直方图面积插值，`StatPercentileAnimation.tsx:266` 却称"样本数据按升序排列" |
| C45 | `data/builders/transcendental.ts:381` | 「泰勒一阶展开渊源」出现在推导链附近 |

---

## 第三部分：改进建议（仅建议，未改动任何代码）

### 3.1 门禁侧（优先级最高，收益最大）

**建议 1｜把审计范围改为全库（1 行改动）**
将 `package.json` 的 `audit` / `audit:strict` 目标从默认 `src/features` 改为显式 `src`，或在 `runner.mjs` 中去掉"仅当 target 恰为 src/features 才补扫"的特例，改为"始终补扫 `src/components` 与 `src/data`"。
预期：门禁从「502 文件 / 0 违规」变为「778 文件 / 560 违规」，`--strict` 真实 exit 1。

**建议 2｜先做规则收敛，再谈清零**
`left/param-label-format` 需要区分 `ParamMeta.labelFormula`（参数标签，适用）与 `ParamMark.labelFormula`（滑块刻度，不适用）。否则 347 处噪音会把真实问题淹没。建议先把 556 处按"真阳性 / 误报"二分，再设定清零目标。

**建议 3｜补一条"超纲词门禁"规则（复用已有机制）**
`syncContract.test.ts` 已在用 `forbiddenTheoremKeywords` 黑名单。建议在 `discipline.mjs` 新增 `discipline/no-beyond-syllabus-terms`，对 `builders/`、`registries/`、`features/**/meta.ts`、`knowledgeTree/` 扫描关键词：
`洛必达 / L'Hôpital / 麦克劳林 / 泰勒展开 / 琴生 / 凹凸性 / 拐点 / 极点极线 / 行列式 / 克拉默 / 外积 / 叉积 / 夹逼 / 等价无穷小 / 上确界 / 下确界 / 紧致 / 无穷级数 / S_∞ / 数列极限 / 特征方程 / 端点效应 / 马尔可夫链 / 卡方分布 / 概率密度函数 / 参数方程 / 极坐标`。
命中即 error，并允许用 `// audit-disable-next-line` + 显式 `importance: "extend"` 双条件豁免。

**建议 4｜把"是否是拓展内容"变成类型系统可表达的事实**
`KnowledgeNode`（`types.ts:39-64`）建议新增 `syllabus: { book: "必修一"|"必修二"|"选择性必修一"|... ; status: "正文"|"选学"|"拓展"|"竞赛" }` 字段，并在 `knowledgeTree.test.ts` 增加断言：
- 每个节点的 `syllabus.book` 必须非空；
- `syllabus.status !== "正文"` 的节点，其 `importance` 必须为 `"extend"`；
- `importance === "extend"` 的节点，页面必须有"拓展/选学"徽标。
这条测试能一次性把 2.2 节列出的 20+ 处超纲定位问题变成机器可拦截项。

**建议 5｜CI 与 husky 补齐**
- `ci.yml`：`audit:strict` 改为全库；`eslint` 加 `--max-warnings 0`；把 `playwright test` 纳入（目前 e2e 有 1 个 spec，10 个用例，完全闲置）。
- `.husky/pre-commit`：`tsc -b` 建议改 `tsc -b --force` 或直接 `tsc --noEmit`，避免 tsbuildinfo 缓存导致空跑。
- `tsconfig.json:30` 的 `include` 建议纳入 `e2e`、`scripts`（目前 CI 的 `tsc -p tsconfig.json` 不覆盖它们）。

**建议 6｜文档口径修正**
`AGENTS.md:76` 与 `TODO_deferred.md:4` 的"全库 0 违规"需改为"`src/features` 0 违规"，或在实际做到全库清零后再恢复"全库"表述。

### 3.2 内容侧（建议排期）

**P0（建议立即修，纯错误不改设计）**
1. `trigLines.ts:89` 第四象限 → `(sin<0, cos>0, tan<0)`
2. `transcendental.ts:288,292,520` 删除 `(x²-1)/2 ≤ x-1` 链与"比线性切线更贴合"的理由
3. `ArithmeticPage.tsx:169` "极小值尖点" → "折线斜率在变号点取最小（拐点）"
4. `set.ts:84` 删除"非空"限定
5. `complex.ts:190` 补"允许相差 2kπ"

**P1（超纲定位，建议统一方案一次性处理）**
建议引入"三层分册方案"，而不是逐页打补丁：
- **层一（教材正文）**：`importance ∈ {basic, core, gaokao}`，页面不显示拓展徽标；
- **层二（教材选学）**：`importance: "extend"` + 徽标「选学」，如复数三角表示、贝叶斯公式、圆锥曲线第二定义；
- **层三（高考拓展/强基竞赛）**：`importance: "extend"` + 徽标「拓展·超出课标」，如洛必达/泰勒、二阶导凹凸、琴生、极值点偏移、齐次化、阿基米德三角形、直线参数 t、马尔可夫链、极化恒等式、阿波罗尼斯圆、奔驰定理、特征方程法。

`derivative-endpoint-taylor` 是当前最需要裁决的一页：其内容（洛必达 + 麦克劳林）100% 属大学微积分，却被放在"导数及其应用"正文知识树、`examWeight: 5`、右屏称"母体源头"。建议二选一：(a) 整页 `importance: "extend"` 并迁移到 `knowledgeTree/challenge.ts` 微专题；(b) 只保留"端点效应 + 二阶导判导函数单调性"的课标内写法，删除洛必达/泰勒模式。

**P2（严谨性，可批量）**
- 参数安全契约：给 `conicHomogenization` / `lineParamT` 补 a>b 钳制，给翻折角改开区间 (0°,180°)，给折叠模型加 a>b 约束；
- 浮点尾零：统一走 `formatMathNumber()`（`utils/mathFormat.ts` 已有，`ConicParamAnimation.tsx:204` 是正确范例）；
- 设问：按 `discipline-specs.md:118-127` 的黑名单/白名单重写 2.3 节 C22–C28 七处；
- 元数据：修正 `setLogic.ts` 的 `gaokaoTopic`，修正 `nike`/`quadratic`/`trig` 的 `chapter` 归属。

### 3.3 一个"根本性"建议

用户偏好根本性方案。就本项目而言，**根本矛盾是：门禁管"怎么画"，但不管"画什么"**。

现有 30 条规则 + 1015 个测试，全部围绕"渲染正确、布局正确、色彩正确、不崩"。而"这页数学内容属不属于高中"这一最核心的约束，**既无字段承载、无规则校验、无测试断言、也无文档维护**（`badge` 手写册次覆盖不足 1/3）。

因此最有效的一步不是逐页修 bug，而是：
1. 给 `KnowledgeNode` 加 `syllabus` 字段（建议 4），把"课标定位"变成 SSOT 数据；
2. 加一条超纲词门禁（建议 3），让"写超纲内容"变成机器可拦截的违规；
3. 把审计范围扩到全库（建议 1），让门禁真正生效。

做完这三步，2.2 节里 20 多处超纲问题会**从"人肉审查才能发现"变成"提交时自动拦截"**——这才是不会反复复发的修法。

---

## 附录 A：审查执行记录

| 命令 | 结果 |
|:---|:---|
| `node .agents/skills/math-page-audit/scripts/audit_page.mjs --strict`（默认 `src/features`） | 502 文件 / 0 error / **exit 0** |
| `... audit_page.mjs src/components --strict` | 91 文件 / **2 error** / exit 1 |
| `... audit_page.mjs src --strict` | 778 文件 / **560 error** / exit 1 |
| `npx tsc -b` | **exit 0**（注：可能命中 tsbuildinfo 缓存） |
| `npx vitest run` | **109 文件 / 1015 用例全部通过**，21.90s |
| `npx eslint src/`（CI 口径） | 未阻断（配置中 `no-explicit-any` / `no-unused-vars` 均为 warn） |
| Playwright e2e | CI 中未接入 |

## 附录 B：本次审查确认"合规"的项（避免误伤）

为使整改不扩大化，以下项经逐条核对**未发现问题**，建议保持不动：

- **集合与逻辑**：德摩根律否定（`math/quantifiers.ts:164-165,209-210`）、`p⇒q ⟺ A⊆B` 方向（`builders/set.ts:82`）、绝对值不等式区间写法
- **函数**：奇偶性均先声明"定义域关于原点对称"（`ParityPage.tsx:80/89/106/115`）、`√x` 非奇非偶判定（`math/function.ts:94-100`）、"同增异减"已列定义域前提（`funcComposite.ts:186-188`）、基本不等式"一正二定三相等"完整（`inequalityBasic.ts:196-197,208`）、半圆证明 CD=2ab/(a+b) 正确
- **三角**：诱导公式各象限符号（`trigIdentity.ts:265-397`）、变换平移量"先平移 |φ| / 先伸缩 |φ|/ω" **处理正确**（`trigTransform.ts:355-359`，这是高频易错点）、辅助角 φ 由 (a,b) 象限定（`trigFormulas.ts:319-326`）、锐角△ `90°-A<B<90°`（`triangleExtrema.ts:116-117`）
- **向量与复数**：投影数量/投影向量正负（`builders/vectorDotProduct.ts:202-204`）、极化恒等式 `¼(|a+b|²-|a-b|²)`、复数 `z·z̄=|z|²`
- **数列**：`Sₙ = (d/2)n² + (a₁-d/2)n` 无常数项、片段和公差 `k²d`、等比 q=1/q≠1 分段齐备、错位相减严格右错 1 格且保留首尾、裂项保留项正确、累加/累乘/倒数/不动点值 `c=q/(1-p)` 正确、**离散点域规范总体合规**（连续函数均以虚线包络，整点用散点）
- **导数**：导数定义式规范（`builders/derivative.ts:125`）、切线方程正确、`f'(x)>0` 充分不必要 与 `递增⟺f'≥0 且不恒为 0` 区分清楚（`:284-290,342`）、极值必要（费马）与充分（变号）区分清楚、各放缩式取等条件均已写明（`transcendental.ts:256/278/313`）
- **解析几何**：p>0 且焦点/准线标齐（`ParabolaScene.tsx:105-114,495-500`）、双曲线渐近线（`ConicPropertiesScene.tsx:145`、`ConicLineScene.tsx:293`）、弦长公式 `√(1+k²)|x₁-x₂|`、`Δ>0` 前提、**斜率预设最大值 1.2（无 |k|>10 违规）**
- **立体几何**：球半径公式全部正确（`circumInSphere.ts` / `polyhedronSphere.ts` / `advancedSphereModels.ts:110-116`：正四面体 R=√6a/4、r=√6a/12，比 3:√3:1）、截面 2/3/4 步**因题自适应而非硬编码**（`sectionConstruction.ts:309-324`）、未发现用 `Vector3DArrow` 画棱、未发现顶点用 Unicode 下标、二面角双垂线在向量法面板齐备（`spatialAngle.ts:205-214`）
- **概率统计**：排列组合全程 `C_n^m`/`A_n^m`/`\binom`（**无 nCr/nPr**）、卡方公式与临界值 2.706/3.841/6.635/10.828 与新课标一致（`builders/pairedData.ts:364-370`）、预设 χ² 计算正确（43.2 / 9.6 / 24.56 / 31.37 / 0）、回归 `b`/`a` 公式与"必过样本中心点"、`E(aX+b)`/`D(aX+b)`、`B(n,p)` 的 `E=np, D=np(1-p)`、正态标准化与 3σ 概率 0.6827/0.9545/0.9973、超几何→二项的课标内表述
- **架构**：全库无 `BrowserRouter`；`src/math` / `src/math3d` 未被门禁报出纯洁性问题
