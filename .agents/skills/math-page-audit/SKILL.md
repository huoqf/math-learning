---
name: math-page-audit
description: 高中数学教学与新高考规范审计 / 页面质量检查 / 解析几何与函数几何核查 / 交互拖拽与三屏同步质检 / 检查页面是否满足教学与新高考 / 审查数学动画页面 / 修复坐标乱飞与公式重影
---

# 高中数学可视化页面质量与新高考教学审计规范 (Math Page Audit Protocol)

> 本技能为高中数学（函数导数、解析几何、平面向量、立体几何、概率统计等）页面质量与教学严谨性的**通用审查准则与 SOP 指南**。
> 适用于全学科所有专题页面的新建、重构与质量核查。

---

## 🎯 核心审查最高总纲：高中数学与新高考绝对合规性

```
┌───────────────────────────────────────────────────────────────────────────────┐
│ 【最高铁律】页面审查首要必须 100% 符合高中数学课标、满足新高考标准！          │
│ 1. 公式与代数：严禁代码变量名污染，正负号与系数严格符合高考消元规范           │
│ 2. 色彩与绑定：三位一体（滑块-图形-公式-看板）色彩绑定，严禁硬编码 Hex        │
│ 3. 动画与作图：虚实线/角弧/向量箭头与演化过程严格符合高中教材作图范式         │
│ 4. 定理与前提：严密准确，所有定理公式必须显式给出数学适用前提条件             │
│ 5. 新高考导向：直击高考大题与秒杀模型（分点比/极化/点差/同构/分类讨论）       │
│ 6. 数形强联动：左滑块/选项 ⇄ 中屏几何图形 ⇄ 右屏看板 100% 实时同步           │
│ 7. 交互与避让：杜绝坐标二次转换乱飞、杜绝标签相交重叠与白框遮断线条           │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## 📐 一、数学严谨性与代数定理规范（代数与定理层）

### 1.1 学术符号与代数消元标准（高考阅卷级）
- **符号纯正性**：严格使用教材规范符号（向量 $\vec{a}$，点标大写字母 $A, P$，参数 $k, b, a, \lambda, x_0$），**严禁将程序员变量名（如 `xa, det, vecA`）暴露在界面上**；
- **代数标准消元**：正负号必须规范（`x - y - 2 = 0`，严禁 `+ -` 出现）；系数为 1 省略（$1x \to x$），系数为 0 整项消去；
- **KaTeX 动态色彩 Token 绑定（铁律 4C）**：严禁硬编码 Hex，必须使用 Token 指令：`\color{${MATH_COLORS.paramPrimary}}{a}x^2`；
- **全屏数学表达式 KaTeX 排版规范**：
  - ❌ **严禁**在普通 JSX 字符串、TipCard 文字或 `description` 中裸写 LaTeX 源码（如 `"e^{x-1} \ge x"` 导致大括号 `{}` 暴露）；
  - ✅ **必须**在 TipCard 等文本中使用 `<KatexFormula formula="..." mode="inline" />` 渲染；`SelectGrid` 的公式必须置于 `formula` 字段，`description` 仅用于 $\le 6$ 字的纯中文微描述。

### 1.2 定理前提完备与多表征覆盖
- **适用前提显式完备**：右屏 `theorems` 必须显式给出前提 `condition`（如：截距式 $a, b \neq 0$；向量基底不共线；斜率存在）；
- **多表征完备探究**：直线 5 种方程形式、圆锥曲线标准/参数方程、向量平行四边形/三角形法则等课标表征必须全量支持；
- **大招提炼（`gaokaoPoints`）**：提炼几何代数速算法（弦心距勾股法、极化恒等式、点差法、同构放缩等）；
- **分类讨论响应（`warnings` 与多临界解耦）**：退化临界（斜率不存在、判别式 $\Delta \le 0$、半径 $r \le 0$）实时警示；当模式包含多个子模型时（如定点相切 $a=1$ 与原点相切 $a=e$），Warning 判定必须按 `subMode` 分支解耦计算，杜绝单一阈值穿透。

---

## 🎛️ 二、左屏控制台交互与教学引导规范（控制层）

### 2.1 单向操作动线与学科特化命名
```
1. 核心专题模式 (SelectGrid 2x2 或 2+1)
   └── 2. 学科特化预设 (SelectGrid 2x2，首项固定为 free 自由探究，微描述 <= 6 字)
         └── 3. 模式特性/锁定量配置 (可选开关)
               └── 4. 动态自适应参数调节 (ParamControl + 重置按钮，按 group 聚合)
                     └── 5. 高考破题与启发导引 (置于最底部辅助区，KatexFormula 渲染)
```
- **学科特化预设命名指引**：
  - **函数与导数**：板块标题使用 `【高考典型切点】` / `【高考临界构型】` / `【常用放缩变体】`；
  - **解析几何**：板块标题使用 `【典型几何构型】` / `【高考定点定值】` / `【特殊位置预设】`；
  - **立体几何**：板块标题使用 `【空间模型底模】` / `【高考高频几何体】` / `【三视图预设】`。

### 2.2 动态定义域保护与参数对象化
- **模式自适应定义域动态保护（铁律）**：`paramConfigs` 必须根据 `activeMode` 动态计算各参数的 `min / max / step / marks`（如对数函数强制保护 $x_0 \in [0.1, 3.5]$，严禁沿用指数负数范围造成定义域穿透误导）；
- **参数对象化聚合**：同一几何对象（如定点 $P_0(x_0, y_0)$、向量分量、复数实虚部）配置相同 `group` 属性，主控动参数在上，底模几何参数在下；
- **自适应排版**：短文本采用 2 列网格，长描述/复杂分式采用单列全宽（`fullWidth: true`），杜绝 `...` 省略号截断。

### 2.3 教学引导卡片规范（底部辅助区）
- **摆放位置**：必须置于左屏最底部，杜绝插在模式与滑块中间打断调参；
- **双要素架构**：①【**几何特征/基础条件**】（$\le 1$ 句）+ ②【**探究设问/破题设问**】（启发性核心思考题，公式用 `<KatexFormula>` 渲染）；
- **职责隔离**：严禁在左屏堆砌定理证明或高考考点（100% 归位右屏 `MathPanel`）。

---

## 🖼️ 三、中屏数形结合与作图范式（视觉与渲染层）

### 3.1 高中数学作图范式与纯净视界
- **虚实线分工**：主体几何图形/主向量/函数曲线为**实线**；辅助线/投影线/延长线/不可见棱为**虚线**；
- **几何符号规范**：垂直处必带直角折线标尺 ∟ 与垂足 $H$（标尺边长固定为 9~10px，杜绝乘 `fontScale` 畸变）；空间角/平面角必配标准角弧与符号；
- **纯净坐标系**：`CoordinateGrid` 默认 `showGrid={false}`（纯白底色 + 清晰 $xOy$ 轴，杜绝方格网干扰）；
- **曲线/直线方程就近标注**：坐标系内重要曲线旁清晰标注方程（如 $y=e^x$、$y=x+1$、$y=\ln x$），符合高中教材与试卷范式。

### 3.2 点标规范与几何避让（杜绝浮点数与重叠遮挡）
- **点标学术化（严禁浮点坐标堆砌）**：
  - ❌ **严禁**在画布动点旁渲染随拖拽跳动的浮点数字符串（如 `P(0.7, 2.01)`，破坏几何纯净性）；
  - ✅ **必须**使用纯代数符号（如动切点 $P$ 或 $P(x_0, y_0)$，交点 $P_1, P_2$）或特征定点坐标（如 $(0, 1)$、$(1, 0)$、$(1, e)$），精确数值 100% 归位右屏看板；
- **单源避让与微描边**：`InteractivePoint` 不传 `label`，所有点文本由 `avoidLabelOverlap` 调度，文字带微描边，严禁添加 `<rect>` 白底框遮断线条；
- **相交与共线图元分流**：相交线段使用 `labelPositionRatio`（如 0.8 与 0.75）错开中心交点；共线/平行图元使用 `getNormalOffset` 沿法向两侧分流。

### 3.3 交互解耦与 1-to-1 拓扑同步
- **坐标解算一致性**：`InteractivePoint` 回调已是数学坐标，**严禁再次调用 `designToMath`**（避免二次转换乱飞）；
- **拖拽预设解耦**：画布拖拽动点时自动切回【自由探究】（`free`）并展开全量参数；
- **预设-中屏拓扑 1-to-1 强同步**：左屏每一项典型预设（如平移变体、二次放缩、相切临界），中屏 `Scene.tsx` 必须有对应的数学方程、差值阴影或特征图元独立分支，杜绝“预设改了参数而中屏拓扑未变”的假同步。

---

## 📊 四、右屏看板同步与 CLI 门禁 5 步审计 SOP（验证层）

### 4.1 四项看板同步与教学严谨性合格标准
- **数学量（Quantities）**：严格反映高中数学核心特征量与模型衍生计算指标（如弦长/离心率/空间角/距离/数量积/切线方程/零点极值等），**严禁与左屏输入滑块参数机械重复**，完整体现从输入自变量到导出特征量与不变量的解算过程；
- **定理（Theorems）**：公式与代数表达式严格符合高中统编教材规范与新高考阅卷标准，显式给出完备的数学适用前提；随左屏一级模式动态置顶核心定理，并随二级预设/特性选项自适应呈现对应的高频变体公式与特化前提；
- **考点（GaokaoPoints）**：直击新高考核心通法、大题破题思维与高频秒杀模型（同构放缩/极化恒等式/点差法/动系定点/分类讨论等）；
- **警示（Warnings）**：退化临界（无定义/除零/判别式 $\Delta \le 0$/破坏命题恒成立）实时响应，多子模型临界精准解耦，正常时自动隐藏。

### 4.2 静态检索特征码（CLI 门禁必查）
```bash
# 门禁 1：排查是否存在画布点标动态浮点数/模板字符串拼接（杜绝坐标堆砌）
grep -rn "label=.*toFixed" src/features/
grep -rn "label={\`.*\\\${" src/features/

# 门禁 2：排查是否存在拖拽二次转换 BUG
grep -rn "onDrag.*designToMath" src/features/

# 门禁 3：排查是否存在非标准 LaTeX 负号拼接
grep -rn "\+ \-" src/features/

# 门禁 4：排查是否存在未替换的硬编码 Hex 颜色
grep -rn "fill=\"#" src/features/
grep -rn "stroke=\"#" src/features/

# 门禁 5：全量编译与单测验证
npx tsc -b
npm test
```

### 4.3 标准化页面审计 5 步执行法（SOP）
1. **第 1 步【查大纲与数学完备度】**：确认核心概念多表征、定理推导与高考秒杀解法无遗漏，运算法则方向性正确。
2. **第 2 步【查左屏动线与状态矩阵】**：
   - 建立【预设-拓扑 1-to-1 状态核查表】，确认左屏每个预设 key 在中屏均有独立对应的数学拓扑与阴影分支；
   - 检查 `paramConfigs` 是否按模式动态保护定义域，教学引导公式是否接入 `<KatexFormula>`。
3. **第 3 步【查中屏图象与作图标注】**：运行 CLI 门禁命令，确认无浮点坐标堆砌、无文字重叠，曲线方程就近标齐，拖拽自动切回 `free`。
4. **第 4 步【查右屏看板与色彩绑定】**：确认公式色彩 Token 绑定一致，核心定理随模式动态置顶，Warning 精准判定各子模式临界。
5. **第 5 步【全量工程构建】**：执行 `npx tsc -b` 与 `npm test`，确保 `Exit code 0`，零类型报错与零警告。
