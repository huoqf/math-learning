import type { ParamMeta } from "../types";
import { MATH_COLORS } from "@/theme";

export const defaultParams: Record<string, number> = {
  h: 1.0,
  omega: 1.0,
  k: 0.5,
  A: 1.5,
};

export const paramMeta: Record<string, ParamMeta> = {
  h: {
    key: "h",
    label: "水平平移量 h",
    labelFormula: "h",
    min: -3.0,
    max: 3.0,
    step: 0.5,
    defaultValue: 1.0,
    group: "水平变换 (x 轴)",
    importance: "core",
    description: "图象沿 x 轴左右平移：h > 0 右移，h < 0 左移（左加右减）",
    descriptionFormula: `y = f(x - \\color{${MATH_COLORS.paramPrimary}}{h})`,
    marks: [{ value: 0, label: "0", labelFormula: "0" }],
  },
  omega: {
    key: "omega",
    label: "横向伸缩 ω",
    labelFormula: "\\omega",
    min: 0.2,
    max: 3.0,
    step: 0.2,
    defaultValue: 1.0,
    group: "水平变换 (x 轴)",
    importance: "advanced",
    description: "图象横向伸缩：ω > 1 压缩为原图的 1/ω，0 < ω < 1 拉伸",
    descriptionFormula: `y = f(\\color{${MATH_COLORS.paramSecondary}}{\\omega} x)`,
    marks: [
      {
        value: 0,
        variant: "critical",
        label: "0 (退化)",
        labelFormula: "0",
      },
      { value: 1, label: "1 (基准)", labelFormula: "1" },
    ],
  },
  k: {
    key: "k",
    label: "竖直平移量 k",
    labelFormula: "k",
    min: -3.0,
    max: 3.0,
    step: 0.5,
    defaultValue: 0.5,
    group: "竖直变换 (y 轴)",
    importance: "core",
    description: "图象沿 y 轴上下平移：k > 0 上移，k < 0 下移（上加下减）",
    descriptionFormula: `y = f(x) + \\color{${MATH_COLORS.paramSecondary}}{k}`,
    marks: [{ value: 0, label: "0", labelFormula: "0" }],
  },
  A: {
    key: "A",
    label: "纵向伸缩 A",
    labelFormula: "A",
    min: -2.5,
    max: 2.5,
    step: 0.5,
    defaultValue: 1.5,
    group: "竖直变换 (y 轴)",
    importance: "core",
    description: "图象纵向拉伸与翻转：|A| > 1 拉伸，A < 0 沿 x 轴翻转",
    descriptionFormula: `y = \\color{${MATH_COLORS.paramPrimary}}{A} \\cdot f(x)`,
    marks: [
      {
        value: 0,
        variant: "critical",
        label: "0 (退化)",
        labelFormula: "0",
      },
      { value: 1, label: "1 (基准)", labelFormula: "1" },
      { value: -1, label: "-1 (翻转)", labelFormula: "-1" },
    ],
  },
};
